extends layout

block content
  h1= message
  
  section(class="card")
    img(class= "cover",src="/images/notfound.jpg")
    h2= error.status
    p We are still working on the site.
    code #{error.stack}
